#!/bin/bash
# =================================================================================================
# 🏢 ENTERPRISE GIT HOOK FRAMEWORK INITIALIZER (CERTIFIED BRANCH)
# -------------------------------------------------------------------------------------------------
# Purpose : Create a self-enforcing .githooks structure that:
#            • Validates JIRA IDs on commits and pushes
#            • Blocks rebases on protected branches
#            • Re-activates hooks automatically after merges and checkouts
#            • Self-bootstraps for all future clones of the 'certified' branch
#            • Updates or creates a README.md with hook details
#
# Usage   : Run this script ONCE at the root of your local repo.
# Commit  : Only the `.githooks` folder (and updated README.md) should be pushed to 'certified'.
# =================================================================================================

echo ""
echo -e "\033[1;36m═══════════════════════════════════════════════════════════════════════════════\033[0m"
echo -e "🚀  Initializing Enterprise Git Hook Enforcement Framework"
echo -e "\033[1;36m═══════════════════════════════════════════════════════════════════════════════\033[0m"
echo ""

# -----------------------------------------------------------------------------------------------
# STEP 1 — Create directory structure
# -----------------------------------------------------------------------------------------------
mkdir -p .githooks
echo -e "📁 Created \033[1;33m.githooks/\033[0m directory for version-controlled hooks."

# -----------------------------------------------------------------------------------------------
# STEP 2 — Commit Message Hook (commit-msg)
# -----------------------------------------------------------------------------------------------
cat > .githooks/commit-msg <<'EOF'
#!/bin/bash
# -----------------------------------------------------------------------------
# Enforces presence of a valid JIRA ID in each commit message.
# -----------------------------------------------------------------------------
commit_msg_file="$1"
commit_msg=$(cat "$commit_msg_file")
jira_pattern='\b(AC|TO|BUG|ICR|DEPO)-[0-9]{3,6}\b'

# Skip merge/version bump commits
if echo "$commit_msg" | grep -Eq "^Merge|Bump version"; then
  exit 0
fi

if ! echo "$commit_msg" | grep -Eq "$jira_pattern"; then
  echo -e "\033[1;31m❌ ERROR:\033[0m Commit message must include a valid JIRA ID (e.g., AC-234567)" >&2
  echo -e "\033[1;33m💡 Example:\033[0m AC-234567 | Enhanced Claims Validation" >&2
  exit 1
fi
EOF

# -----------------------------------------------------------------------------------------------
# STEP 3 — Post-Checkout Hook
# -----------------------------------------------------------------------------------------------
cat > .githooks/post-checkout <<'EOF'
#!/bin/bash
git config core.hooksPath .githooks
echo -e "\033[1;34m🪝 Hooks active (.githooks)\033[0m"
EOF

# -----------------------------------------------------------------------------------------------
# STEP 4 — Post-Merge Hook
# -----------------------------------------------------------------------------------------------
cat > .githooks/post-merge <<'EOF'
#!/bin/bash
git config core.hooksPath .githooks
echo -e "\033[1;32m🔁 Hooks reactivated after merge.\033[0m"
EOF

# -----------------------------------------------------------------------------------------------
# STEP 5 — Pre-Rebase Hook
# -----------------------------------------------------------------------------------------------
cat > .githooks/pre-rebase <<'EOF'
#!/bin/bash
protected="Release_QA|Certified|main|master"
branch=$(git rev-parse --abbrev-ref HEAD)
if echo "$branch" | grep -Eq "$protected"; then
  echo -e "\033[1;31m🚫 Rebase not allowed on protected branch: $branch\033[0m"
  exit 1
fi
EOF

# -----------------------------------------------------------------------------------------------
# STEP 6 — Pre-Push Hook
# -----------------------------------------------------------------------------------------------
cat > .githooks/pre-push <<'EOF'
#!/bin/bash
jira_pattern='\b(AC|TO|BUG|ICR|DEPO)-[0-9]{3,6}\b'
msg=$(git log -1 --pretty=%B)

if ! echo "$msg" | grep -Eq "$jira_pattern"; then
  echo -e "\033[1;31m❌ Push blocked:\033[0m missing valid JIRA ID (e.g., AC-123456)"
  exit 1
fi

echo -e "\033[1;32m✅ Push validation passed.\033[0m"
EOF

# -----------------------------------------------------------------------------------------------
# STEP 7 — Pre-Commit Hook
# -----------------------------------------------------------------------------------------------
cat > .githooks/pre-commit <<'EOF'
#!/bin/bash
if ! git diff --cached --name-only | grep -q .; then
  echo "⚠️  No staged files to commit."
  exit 0
fi
echo "🔍 Pre-commit check passed..."
EOF

# -----------------------------------------------------------------------------------------------
# STEP 8 — Bootstrap Hook
# -----------------------------------------------------------------------------------------------
cat > .githooks/bootstrap.sh <<'EOF'
#!/bin/bash
# -----------------------------------------------------------------------------
# AUTO-BOOTSTRAP: Enables .githooks automatically for every clone/checkout
# -----------------------------------------------------------------------------
repo_root="$(git rev-parse --show-toplevel 2>/dev/null)" || exit 0
mkdir -p "$repo_root/.git/hooks"

cat > "$repo_root/.git/hooks/post-checkout" <<'INNER'
#!/bin/bash
repo_root="$(git rev-parse --show-toplevel 2>/dev/null)" || exit 0
if [ -d "$repo_root/.githooks" ]; then
  git -C "$repo_root" config core.hooksPath .githooks
  echo -e "\033[1;34m🪝 Project hooks activated (.githooks)\033[0m"
fi
exit 0
INNER

chmod +x "$repo_root/.git/hooks/post-checkout"
git -C "$repo_root" config core.hooksPath .githooks
echo -e "\033[1;32m✅ .githooks auto-bootstrapped successfully.\033[0m"
EOF

# -----------------------------------------------------------------------------------------------
# STEP 9 — Permissions & Activation
# -----------------------------------------------------------------------------------------------
chmod +x .githooks/*
git config core.hooksPath .githooks

echo ""
echo -e "\033[1;32m✅ All Git hooks have been generated and activated locally.\033[0m"

# -----------------------------------------------------------------------------------------------
# STEP 10 — Create or Update README.md
# -----------------------------------------------------------------------------------------------
repo_name=$(basename "$(git rev-parse --show-toplevel 2>/dev/null)")
readme_path="README.md"

echo ""
echo -e "🧾 Updating repository documentation..."

if [ ! -f "$readme_path" ]; then
  echo -e "📘 No README found. Creating a new one..."
  cat > "$readme_path" <<EOF
# $repo_name

This repository includes an automated **Git Hook Enforcement Framework** for maintaining consistent commit hygiene and traceability standards.

---

## 🔒 Git Hook Enforcement Policy

The following local Git hooks are automatically installed and enforced when you clone or checkout the **certified** branch:

| Hook | Purpose |
|------|----------|
| \`commit-msg\` | Ensures every commit includes a valid JIRA ID (e.g., AC-234567) |
| \`pre-push\` | Blocks pushes without valid JIRA IDs |
| \`pre-rebase\` | Prevents rebasing on protected branches |
| \`post-merge\` / \`post-checkout\` | Keeps hooks always active |
| \`bootstrap.sh\` | Self-installs hooks after clone |

---

### 🧠 Usage Notes

1. These hooks are automatically enabled — no manual setup required.  
2. Supported JIRA ID patterns: **AC-123456**, **ICR-54321**, **BUG-999**, etc.  
3. For feature branches, prefix your commits with the corresponding JIRA ID.

---
EOF
else
  echo -e "\n## 🔒 Git Hook Enforcement Policy\n" >> "$readme_path"
  echo "This repository enforces automated Git hooks for JIRA ID validation, branch protection, and consistent commit hygiene under the **certified** branch." >> "$readme_path"
fi

echo -e "\033[1;32m✅ README.md updated with Git hook details.\033[0m"

# -----------------------------------------------------------------------------------------------
# Completion Message
# -----------------------------------------------------------------------------------------------
echo ""
echo -e "\033[1;36m───────────────────────────────────────────────────────────────────────────────\033[0m"
echo -e "📦  Next Steps:"
echo -e "   1️⃣  Review the generated hooks under \033[1;33m.githooks/\033[0m"
echo -e "   2️⃣  Commit and push only the \033[1;33m.githooks/\033[0m folder and \033[1;33mREADME.md\033[0m to the \033[1;32mcertified\033[0m branch."
echo -e "   3️⃣  When anyone clones or checks out \033[1;32mcertified\033[0m,"
echo -e "       the hooks will self-bootstrap and enforce automatically. 💪"
echo -e "\033[1;36m───────────────────────────────────────────────────────────────────────────────\033[0m"
echo ""
