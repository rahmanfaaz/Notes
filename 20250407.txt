/* Force the ENV + USER widgets inside the blue bar */
#top-env-user-wrapper {
    position: absolute;
    top: 12px;          /* vertical alignment inside blue header */
    right: 22px;        /* align to right */
    z-index: 10000;
    display: flex;
    gap: 14px;
    width: auto;
    background: transparent;
}

/* Remove the vertical space they originally created */
#top-env-user-wrapper + div {
    display: none;
}

/* Shrink Streamlit widget sizes to fit in header */
#top-env-user-wrapper .stSelectbox, 
#top-env-user-wrapper .stTextInput {
    width: 180px !important;
}

/* Adjust internal widget padding */
#top-env-user-wrapper input, 
#top-env-user-wrapper select {
    height: 32px !important;
    font-size: 13px !important;
    padding: 4px 8px !important;
}
