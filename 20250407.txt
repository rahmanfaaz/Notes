grep '502' access.log | awk '{
  match($0, /MicSec:([0-9]+)/, m);
  if (m[1]) {
    minutes = m[1] / 60000000;
    printf "%s -> MicSec in minutes: %.6f\n", $0, minutes;
  } else {
    print $0;
  }
}'
