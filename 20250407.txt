/* Right side alignment */
.top-right-controls {
    margin-left: auto;
    display: flex;
    align-items: center;
    gap: 14px;
}

/* Placeholder width (controls widget size) */
#env-slot, #user-slot {
    width: 180px;
}

/* Make widgets shrink inside the bar */
#env-slot .stSelectbox > div,
#user-slot .stTextInput > div {
    margin-top: 0 !important;
}

#env-slot select, 
#user-slot input {
    height: 32px !important;
    padding: 4px 8px !important;
    font-size: 13px !important;
}
