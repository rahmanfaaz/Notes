/* Move ENV + USER into the blue top bar */
#top-right-controls {
    position: absolute;
    top: 10px;           /* adjust vertical placement */
    right: 20px;         /* push to right side */
    z-index: 9999;       /* ensure visibility */
    display: flex;
    gap: 12px;
}

/* Make Streamlit widgets fit in header */
#top-right-controls .stSelectbox, 
#top-right-controls .stTextInput {
    width: 150px !important;
}

/* Remove unwanted extra padding from Streamlit container */
.block-container {
    padding-top: 0 !important;
}
