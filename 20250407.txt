-- Prepare and call the procedure

CALL SFDB_D_ICR_DEP_DW.ODS_DEP_DW.proc_accept_claims(
    ARRAY_CONSTRUCT('CLAIM001', 'CLAIM002', 'CLAIM003'),
    'api_user_faaz',                 -- changed_by
    'Bulk acceptance of claims'      -- reason
);
