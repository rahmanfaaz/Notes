kubectl get pods -n icr-ds-dep --watch-only | grep --line-buffered 'db-ingestion' | while read -r line; do pod=$(echo "$line" | awk '{print $1}'); echo "📦 $pod detected — fetching logs..."; kubectl logs -n icr-ds-dep $pod || kubectl logs -n icr-ds-dep $pod --previous; break; done
