kubectl logs claims-ui-544fc774-mqpv5 | awk -F'[ =,]+' '/sm_user:/ { 
  for (i=1; i<=NF; i++) { 
    if ($i ~ /^sm_user:/) sm_user=$(i+1); 
    if ($i ~ /^rName=/) rName=$(i); 
  } 
  print sm_user, rName 
}'
